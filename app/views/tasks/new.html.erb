<main class="container">
  <%= link_to sanitize("&larr; Back to tasks"), project_path(@project) %>

  <div class="text-2xl font-semibold my-5">
    <h1>New task</h1>
  </div>

  <%= turbo_frame_tag @task do %>
    <%= render "form", submit_text: "Create task", project: @project, task: @task %>
  <% end %>
</main>
