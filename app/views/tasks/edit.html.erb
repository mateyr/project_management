<main class="container">
  <%= link_to sanitize("&larr; Back to projects"), project_path(@project) %>

  <div class="text-2xl font-semibold my-5">
    <h1>Edit task</h1>
  </div>

  <%= turbo_frame_tag @task do %>
    <%= render "form", submit_text: "Update task", project: @project, task: @task %>
  <% end %>
</main>

