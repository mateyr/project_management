<%= turbo_stream.prepend "projects", partial: "project", locals: { project: @project, user_id: current_user.id } %>
<%= turbo_stream.remove_all  "form#new_project" %>
<%= render_turbo_stream_flash_messages %>
