<main class="container">
  <%= link_to sanitize("&larr; Back to collaborators"), project_project_users_path(@project) %>

  <div class="text-2xl font-semibold my-5">
    <h1>New collaborator</h1>
  </div>

  <%= turbo_frame_tag @project_user do %>
    <%= render "form", submit_text: "Create collaborator", project: @project, project_user: @project_user %>
  <% end %>
</main>
